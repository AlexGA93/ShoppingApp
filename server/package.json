{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --port 5000 --routes routes.json --watch db.json",
    "dev": "concurrently \"npm run server\" \"cd ../client && npm start\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.1.0",
    "json-server": "^0.17.0"
  }
}
